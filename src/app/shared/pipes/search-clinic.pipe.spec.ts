import { SearchClinicPipe } from './search-clinic.pipe';

describe('SearchClinicPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchClinicPipe();
    expect(pipe).toBeTruthy();
  });
});
