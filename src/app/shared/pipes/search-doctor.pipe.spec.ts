import { SearchDoctorPipe } from './search-doctor.pipe';

describe('SearchDoctorPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDoctorPipe();
    expect(pipe).toBeTruthy();
  });
});
