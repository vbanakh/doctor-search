import { SearchDistrictPipe } from './search-district.pipe';

describe('SearchDistrictPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDistrictPipe();
    expect(pipe).toBeTruthy();
  });
});
