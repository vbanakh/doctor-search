import { SearchSpecialityPipe } from './search-speciality.pipe';

describe('SearchSpecialityPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSpecialityPipe();
    expect(pipe).toBeTruthy();
  });
});
