import { SearchCityPipe } from './search-city.pipe';

describe('SearchCityPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCityPipe();
    expect(pipe).toBeTruthy();
  });
});
